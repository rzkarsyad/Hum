
# Xcode user state (machine-local, noisy)
*.xcuserstate
xcuserdata/
.DS_Store

# Build / release artifacts
build/
dist/

# Coverage + local probes (not part of the app)
*.profraw
check_mediaplayer2

# Local tooling state (Claude Code / superpowers)
.claude/
.superpowers/
.codex/

# The landing page lives in site/ and is deployed by Vercel from this repo;
# its own .gitignore covers node_modules, .next and out.
.vercel
.env*
